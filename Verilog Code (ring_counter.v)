module ring_counter (
    input clk,
    input reset,
    output reg [3:0] Q
);

always @(posedge clk or posedge reset) begin
    if (reset)
        Q <= 4'b0001;          // Initial state
    else
        Q <= {Q[2:0], Q[3]};   // Rotate left
end

endmodule